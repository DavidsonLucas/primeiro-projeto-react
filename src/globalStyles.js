import { createGlobalStyle } from "styled-components"

 const Tudo = createGlobalStyle`
    *{
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        outline:none ;
        font-family: 'Roboto', sam serifa;
    }

   
`

export default Tudo